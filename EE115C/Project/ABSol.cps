                                                                               
; autoPinSwap() results for schematic network.

swapPins("NOT" "")

swapPins("OR" "(p A B)")

swapPins("AND" "(p A B)")

swapPins("MUXblock" "")

swapPins("NAND2" "(p A B)")

swapPins("XORG" "(p A B)")

swapPins("HAdder" "")
