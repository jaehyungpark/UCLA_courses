                                                                               
; autoPinSwap() results for schematic network.

swapPins("NOT" "")

swapPins("OR" "(p A B)")

swapPins("AND" "(p A B)")

swapPins("NOR" "(p A B)")
