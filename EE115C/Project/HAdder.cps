                                                                               
; autoPinSwap() results for schematic network.

swapPins("NOT" "")

swapPins("AND" "(p A B)")

swapPins("NAND2" "(p A B)")

swapPins("XORG" "(p A B)")
